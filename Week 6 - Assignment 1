hrs = input("Enter hours:")
h = float(hrs)

rte = input("Rate: ")
r = float(rte)

def computepay(h,r):
	if h > 40:
	    total = (h - 40) * (r * 1.5) + (40 * r)
    	else:      
    		total = h * r
	return total

pay = computepay(h,r)
print("Pay", pay)
